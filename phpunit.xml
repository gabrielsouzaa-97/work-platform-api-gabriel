<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Characterization">
            <directory>tests/Characterization</directory>
        </testsuite>
        <testsuite name="Contract">
            <!-- Opt-in via RUN_UPSTREAM_CONTRACT=1. Default-skipped at the example level
                 (see tests/Contract/Customers/UpstreamContractTest.php). NOT included
                 in default test suite to avoid accidental real-SSH calls in CI. -->
            <directory>tests/Contract</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- DB isolation is enforced via TestCase::refreshApplication() (SQLite :memory:).
             phpunit.xml <env> tags cannot reliably override Docker container env vars
             because Laravel's ImmutableRepository reads $_ENV (set at process start)
             before putenv() values. The config() override in TestCase is authoritative. -->
        <!-- Defense-in-depth: force-disable Contract tests in all non-opt-in runs (QA-F5-010). -->
        <env name="RUN_UPSTREAM_CONTRACT" value="0" force="true"/>
        <env name="NC_SUITE_CATALOG_JSON" value="tests/fixtures/suite_catalog.json"/>
    </php>
</phpunit>
