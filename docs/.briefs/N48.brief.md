---
sprint_id: "N48"
planner: "/rock n48"
status: "READY_FOR_PUSH"
---

# Quality Brief — Sprint N48 (ISSUE-057 opção B — fatia API)

> **Escopo parcial**: instalação real da suíte me360 no host exige `work-platform-scripts` (upstream). Esta sprint entrega preparação API + contrato documentado.

## Objetivo API (sem upstream)

1. `cluster_servers.legacy_me360_capable` (bool, default false)
2. `ProvisioningReadinessValidator`: quando cluster `legacy_me360_capable=true` **e** suite catalog local contém `mework360_memail` + `me360_theme` (active), permitir provision legado `suite_catalog` (não 422)
3. Fixture `tests/fixtures/suite_catalog_me360.json` + testes de contrato
4. Runbook cross-repo `docs/runbooks/me360-suite-catalog-b.md` + ISSUE upstream pointer
5. OpenAPI: nota readiness + campo cluster (admin)

## Fora de escopo (upstream)

- Alterar `manage.sh` / `suite_catalog.json` no `work-platform-scripts`
- Canário E2E com instalação real da suíte

## Gate

- Flag false → comportamento F28 inalterado (422)
- Flag true + catalog me360 → 202 em payload default `{}`
- Flag true + catalog sem me360 → 422 (bloqueado até upstream B)
- CI verde
