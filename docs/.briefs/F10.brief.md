---
auditor_subagent_id: "pending"
audited_at: "2026-05-24T05:00:00Z"
findings_count: 0
severity_summary:
  CRITICAL: 0
  HIGH: 0
  MEDIUM: 0
  LOW: 0
audit_evidence_path: "docs/ISSUES.md#issue-014"
sprint_id: "F10"
planner: "/pmo sprint"
status: "READY_FOR_PUSH"
---

# Quality Brief — Sprint F10 (JobLogFetcher argv fix — ISSUE-014)

> **Provenance artifact** (PATTERN-001 / Decision #187). Fast-track pós-triagem `/qa debug` 2026-05-24.

## Identificação

- **Sprint**: F10 — Corrigir argv do `JobLogFetcher` (logs vazios em `/queue/{jobId}`)
- **Origem**: ISSUE-014 (bug MEDIUM) descoberto durante investigação ISSUE-013; sintoma reportado em ISSUE-009
- **Branch**: `fix/issue-014-job-log-fetcher-argv` → merged em `main` (local)
- **Commit**: `197ff46` — `fix(jobs): ISSUE-014 — corrigir argv do JobLogFetcher para introspection job`
- **Roadmap section**: `docs/ROADMAP.md` — `## Sprint F10`
- **Tasks** (3P):
  - `[x] F10.1` — Corrigir argv `JobLogFetcher` (sem client slug)
  - `[x] F10.2` — Testes regressão argv + `SshRemoteException(notImplemented)`
  - `[ ] F10.3` — Push + deploy + validar logs em job novo (gate operacional)

## Causa raiz

`JobLogFetcher` montava `nextcloud-manage <client> job <id> logs --json`. Comandos `job` são **introspection** — sintaxe correta: `nextcloud-manage job <id> logs --json`. Upstream rejeitava com exit 101 `cmd_not_allowed`.

## Escopo implementado

| Arquivo | Mudança |
|---|---|
| `app/Modules/Jobs/Services/JobLogFetcher.php` | argv fix + catch `SshRemoteException(notImplemented)` |
| `tests/Feature/Jobs/JobLogFetcherTest.php` | assert argv + teste exception fallback |
| `tests/Contract/Jobs/UpstreamJobLogsContractTest.php` | comentário contrato corrigido |
| `docs/ISSUES.md` | ISSUE-014 → fixed; ISSUE-009 → mitigated |

## Pré-validação

| Check | Resultado |
|---|---|
| `php artisan test tests/Feature/Jobs/JobLogFetcherTest.php` | **12 passed**, 39 assertions |
| Evidência produção (pré-fix) | `jobs.log_fetch.failed` 100% dos `job.finished` |
| Alinhamento contrato | `JobsPollStuckCommand` + SSH API Reference §3.4 |

## Gate pós-deploy

1. Rodar job qualquer (ex.: `user_delete`) em staging/produção
2. Confirmar `jobs.summary IS NOT NULL` após webhook
3. `/queue/{jobId}` exibe linhas (não "Nenhum log disponível")
4. Log `sshclient` mostra `nextcloud-manage job <uuid> logs --json` com `exit_code: 0`

## Riscos

- **Baixo** — diff isolado, sem mudança de contrato HTTP
- Jobs antigos com `summary` vazio **não** backfillam automaticamente (reenvio webhook ou fetch manual)
