---
verified_at: "2026-07-05T02:19:00-03:00"
sprint_id: "N37"
brief_schema_valid: true
evidence_found: false
status: "PASS"
issues_found: 0
---

# Plan Verifier Report

Sprint: N37  
Planner: planejador-melhorias  
Status: **PASS**  
Brief schema valid: yes  
Evidence found: no (`audit_evidence_path` vazio — aceitável)

## Checklist de verificação

### 1. Frontmatter do brief

| Campo | Resultado |
|-------|-----------|
| `auditor_subagent_id` | Presente (UUID) |
| `audited_at` | `2026-07-05T02:16:00-03:00` (ISO-8601) |
| `findings_count` | `5` |
| `severity_summary` | CRITICAL:0, HIGH:1, MEDIUM:2, LOW:2 (= 5) |
| `audit_evidence_path` | `""` (vazio) |
| `sprint_id` / `planner` | Coerentes com input (`N37`, `planejador-melhorias`) |

- [x] Contagem `findings_count: 5` bate com PB-N37-001 … PB-N37-005 listados
- [x] `severity_summary` soma 5 e classifica corretamente (1 HIGH, 2 MEDIUM, 2 LOW)
- [x] Status do auditor `PASS_WITH_NOTES` compatível (notas são advisories, não bloqueios)
- [x] Campos obrigatórios do schema plan-brief presentes (`schemas/plan-brief.schema.json` ausente no repo; validação manual)

### 2. Paths e constraints executáveis

| Path referenciado | Existe | Notas |
|-------------------|--------|-------|
| `routes/web.php` | sim | L41 = grupo `['auth','active.operator']`; L109–110 = padrão `can:manage-operators` (citação de padrão, rotas `/docs/api` ainda não existem) |
| `resources/views/layouts/app.blade.php` | sim | L116–123: link API Docs **sem** `@can` — confirma PB-N37-001 |
| `docs/openapi-external.yaml` | sim | L41–42 `servers` → prod URL; L5 `version: 1.0.0-draft` |
| `app/Modules/Core/Services/ApiKeyService.php` | sim | L22–46 `generate()`; L33 `scopes ?: null` |
| `app/Http/Livewire/ApiKeys/Index.php` | sim | L29–31 `$rules` (só `createName` hoje); L74 `scopes: null` hardcoded |
| `config/api-scopes.php` | sim | chaves `legacy` + `v1` |
| `app/Http/Middleware/EnsureApiKeyScope.php` | sim | `null`/`*` → irrestrita (L37–39) |
| `vite.config.js` | sim | entrada `dashboard.js` como padrão citado |
| `resources/js/pages/dashboard.js` | sim | padrão Vite existente |
| `package.json` | sim | `@scalar/api-reference` ainda ausente (esperado — sprint não iniciada) |
| `tests/Feature/ApiKeys/ApiKeyTest.php` | sim | sem testes de scopes (confirma PB-N37-003) |
| `resources/views/livewire/api-keys/` | sim | `index.blade.php` presente |

- [x] Constraints são determinísticas (gates, path fixo, `Rule::in`, Vite/npm, badges/testes)
- [x] Nenhum path inventado ou inexistente

### 3. Cobertura de tasks e riscos

| Task | Severidade | Constraint / advisory |
|------|------------|----------------------|
| N37.1 (M) | — | #1 gate duplo, #2 spec fixo, #4 Scalar/Vite |
| N37.2 (M) | — | #3 scopes service + validação, #5 testes |
| N37.3 (P) | — | #5 badges `null`/`*` |
| N37.4 (P) | — | #1 sidebar `@can` + banner/versão (advisory §2–3) |
| PB-N37-001 (HIGH) | sidebar sem gate | Endereçado por constraint #1 + task N37.4 |
| PB-N37-002 (MEDIUM) | try-it-out cross-env | Advisory §1 (disable ou override `servers`) |
| PB-N37-003 (MEDIUM) | testes scopes ausentes | Constraint #5 |
| PB-N37-004 (LOW) | gate duplo | Documentado; alinhado ao padrão do projeto |
| PB-N37-005 (LOW) | `[]` → null | Confirmado em `ApiKeyService.php:33` |

- [x] Cada task **M** (N37.1, N37.2) possui constraint aplicável
- [x] Risco **HIGH** (PB-N37-001) mapeado para N37.4 / constraint #1

### 4. Coerência com código e decisões

- [x] Alinhado a **ISSUE-047** (`docs/ISSUES.md`): viewer privado Scalar, scopes v1, sem `openapi.yaml` interno
- [x] `EnsureApiKeyScope` honra `null`/`*` como irrestrita — consistente com constraint #5
- [x] Brief proíbe ofertar `legacy` scopes — `config/api-scopes.php` tem `legacy`, mas constraint é correta para UI v1
- [x] Advisory try-it-out LAB não contradiz ISSUE-047 fase 2 (proxy autenticado fora de escopo N37)
- [x] Gate `manage-operators` definido em `AppServiceProvider.php` — padrão já usado em rotas existentes

## Divergências

Nenhuma bloqueante identificada.

**Nota informativa (não contada como issue):** a citação `web.php:109–110` aponta para `/api-keys` como **exemplo de padrão** de rota com `can:manage-operators` dentro do grupo auth; as rotas `/docs/api` ainda serão criadas na sprint.
