---
auditor_subagent_id: "4df9a524-cc91-4106-b925-36513e50b673"
audited_at: "2026-07-05T02:16:00-03:00"
findings_count: 5
severity_summary:
  CRITICAL: 0
  HIGH: 1
  MEDIUM: 2
  LOW: 2
audit_evidence_path: ""
sprint_id: "N37"
planner: "planejador-melhorias"
status: "PASS_WITH_NOTES"
---

# Technical Brief — Sprint N37 (ISSUE-047: API Console fase 1)

sprint: N37
modulos: app/Http/Livewire/ApiKeys/Index.php, app/Modules/Core/Services/ApiKeyService.php, app/Http/Middleware/EnsureApiKeyScope.php, config/api-scopes.php, routes/web.php, resources/views/livewire/api-keys/, resources/views/layouts/, docs/openapi-external.yaml, vite.config.js, package.json
gerado_em: 2026-07-05T02:16:00-03:00
valido_ate: fim da sprint N37

## Quality Constraints

1. **Gate duplo em rotas de docs (N37.1, N37.4)** — `/docs/api` e `GET /docs/api/spec` dentro de `['auth', 'active.operator']` + `can:manage-operators` (`web.php:41,109–110`). Repetir `Gate::authorize` no componente. Corrigir sidebar (`app.blade.php:116–123`): `@can('manage-operators')`, label "Documentação API".

2. **Spec só de path fixo (N37.1)** — Ler exclusivamente `base_path('docs/openapi-external.yaml')`. Proibido `public/`, parâmetros de path ou servir `openapi.yaml` interno.

3. **Scopes via service existente (N37.2)** — Manter `ApiKeyService::generate(scopes:)` (`ApiKeyService.php:22–46`). Checkbox vazio → `null`. Validar com `Rule::in(config('api-scopes.v1'))` no `$rules` (`Index.php:29–31`). Não ofertar `legacy`.

4. **Scalar via Vite/npm (N37.1)** — Nova entrada em `vite.config.js` (padrão `dashboard.js`). Spec via URL autenticada `/docs/api/spec`. Sem CDN.

5. **Badges + testes (N37.3, N37.2)** — `null`/`*` → "irrestrita". Estender `ApiKeyTest.php` para scopes.

## Technical Advisory

1. **Try-it-out Scalar** — Spec aponta prod (`openapi-external.yaml:41–42`). Desabilitar try-it-out ou override `servers` para `url('/api/v1')` em LAB.

2. **Versão no banner** — Extrair `info.version` server-side (`1.0.0-draft`).

3. **Banner LAB** — Usar `config('app.env')` ou env dedicado; sem hardcode de host.

## Preventive findings

### [PB-N37-001] HIGH — Sidebar sem gate
**File**: resources/views/layouts/app.blade.php:116–123

### [PB-N37-002] MEDIUM — Try-it-out cross-env
**File**: docs/openapi-external.yaml:41–42

### [PB-N37-003] MEDIUM — Testes de scopes ausentes
**File**: tests/Feature/ApiKeys/ApiKeyTest.php

### [PB-N37-004] LOW — Gate duplo intencional (padrão do projeto)

### [PB-N37-005] LOW — `[]` colapsa para null (`ApiKeyService.php:33`)
